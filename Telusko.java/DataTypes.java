
public class DataTypes {
     public static void main(String[] args) {
        int num1=9;   // when we talk about data types what kind of values we have? here 9 & k is called literals bez literally it hold the values here.
        byte by=127;
        short sh=558;
        long l=5854l;

        float f=5.8f;// if we want to float then we need to mention f.
        double d=5.8;

        char c='k';  // char must put inside the single quote.In place of k we also need to put digit bez digits are also a character.

        boolean b=true;
    // here no output bez we are not printing anything.
     }
    
}